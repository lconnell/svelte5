{
  "name": "backend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun src/index.ts",
    "lint": "echo \"Linting for backend not yet configured\" && exit 0",
    "format": "echo \"Formatting for backend not yet configured\" && exit 0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.15.29"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.1",
    "@hono/zod-openapi": "^0.19.8",
    "@supabase/supabase-js": "^2.49.9",
    "hono": "^4.7.11",
    "jose": "^6.0.11",
    "zod": "^3.25.51"
  }
}
